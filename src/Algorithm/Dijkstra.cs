namespace Belly.Algorithm
{
    public class Dijkstra
    {
        public IDictionary<Node, int> Calculate(Node head)
        {
            Dictionary<Node, int> answer = new();
            answer.Add(head, 0);
            HashSet<Node> selected = new();
            //TODO:
            return answer;
        }
    }
}